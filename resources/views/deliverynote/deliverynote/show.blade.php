@extends('layouts.app')

@section('content')
    <div class="container">
        <div class="row">
            @include('admin.sidebar')

            <div class="col-md-9">
                <div class="panel panel-default">
                    <div class="panel-heading">DeliveryNote {{ $deliverynote->id }}</div>
                    <div class="panel-body">

                        <a href="{{ url('/deliverynote') }}" title="Back"><button class="btn btn-warning btn-xs"><i class="fa fa-arrow-left" aria-hidden="true"></i> Back</button></a>
                        <a href="{{ url('deliverynote' . $deliverynote->id . '/edit') }}" title="Edit DeliveryNote"><button class="btn btn-primary btn-xs"><i class="fa fa-pencil-square-o" aria-hidden="true"></i> Edit</button></a>
                        {!! Form::open([
                            'method'=>'DELETE',
                            'url' => ['deliverynote', $deliverynote->id],
                            'style' => 'display:inline'
                        ]) !!}
                            {!! Form::button('<i class="fa fa-trash-o" aria-hidden="true"></i> Delete', array(
                                    'type' => 'submit',
                                    'class' => 'btn btn-danger btn-xs',
                                    'title' => 'Delete DeliveryNote',
                                    'onclick'=>'return confirm("Confirm delete?")'
                            ))!!}
                        {!! Form::close() !!}
                        <br/>
                        <br/>

                        <div class="table-responsive">
                            <table class="table table-borderless">
                                <tbody>
                                    <tr>
                                        <th>ID</th><td>{{ $deliverynote->id }}</td>
                                    </tr>
                                    <tr><th> Fecha </th><td> {{ $deliverynote->fecha }} </td></tr><tr><th> Proveedor </th><td> {{ \App\Supplier::find($deliverynote->supplier_id)->nombre }} </td></tr><tr><th> Descripcion </th><td> {{ $deliverynote->descripcion }} </td></tr>
                                </tbody>
                            </table>
                        </div>

                    </div>
                </div>
            </div>
        </div>
    </div>
@endsection
